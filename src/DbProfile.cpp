#include "DbProfile.h"

DbProfile::DbProfile(QString hostName, QString databaseName, QString userName, QString password):
	HostName(hostName), DatabaseName(databaseName), UserName(userName), Password(password)
{
}
